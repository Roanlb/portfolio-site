import React from 'react';
import './App.css';
import MainPage from './components/MainPage';

function App() {
  return (
    <div className="App">
      <MainPage />
    </div>
    //make boxes horizontally aligned and of a smaller size. make grid with 10/80/10, then flexbox for middle column with the three boxes
    //i would add some pretty animation somewhere
  );
}

export default App;
